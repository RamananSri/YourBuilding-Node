var fs = require("fs");

var errorMessage = "test";
var logFile = "log.txt";
fs.writeFile
var logErrors = fs.writeFile(logFile, data, (err, data) => {
	console.log(data);
});

module.exports = {
	logErrors
};
